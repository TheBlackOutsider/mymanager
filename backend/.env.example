# Configuration de base
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15

# Base de données
DATABASE_URL=postgresql://hrlead:hrlead123@localhost:5432/hrlead
POSTGRES_DB=hrlead
POSTGRES_USER=hrlead
POSTGRES_PASSWORD=hrlead123

# Redis
REDIS_URL=redis://localhost:6379

# Email (optionnel)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@company.com

# LDAP (optionnel)
LDAP_ENABLED=false
LDAP_SERVER=ldap://your-ldap-server
LDAP_PORT=389
LDAP_BASE_DN=dc=company,dc=com
LDAP_BIND_DN=cn=admin,dc=company,dc=com
LDAP_BIND_PASSWORD=admin-password

# CORS
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]
CORS_CREDENTIALS=true
CORS_METHODS=["GET", "POST", "PUT", "DELETE", "OPTIONS"]
CORS_HEADERS=["*"]

# Services ports
API_GATEWAY_PORT=8000
AUTH_SERVICE_PORT=8001
EMPLOYEE_SERVICE_PORT=8002
EVENT_SERVICE_PORT=8003
LEAVE_SERVICE_PORT=8004
NOTIFICATION_SERVICE_PORT=8005
REPORT_SERVICE_PORT=8006
